import type { Operacion } from '@/types/operacion'

/**
 * Sprint 3.5, parte 9 — reservado para cuando exista IA real que busque
 * patrones entre operaciones. Hoy no se conecta a ninguna pantalla.
 */
export function patterns(_operaciones: readonly Operacion[]): null {
  return null
}
