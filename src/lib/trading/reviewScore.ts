import type { Operacion } from '@/types/operacion'

/**
 * Sprint 3.5, parte 9 — reservado para cuando exista IA real. Hoy no se
 * conecta a ninguna pantalla: "una operación nunca se juzga, solo se
 * observa" (parte 10). Devuelve null a propósito, no un puntaje fingido.
 */
export function reviewScore(_operacion: Operacion): null {
  return null
}
