/**
 * Superficie pública del módulo Mi proyecto. Igual que Notas/Finanzas:
 * `public.ts` solo expone la identidad de navegación — la ruta importa
 * `MiProyectoScreen` directo de su propio archivo (ver App.tsx).
 */
export const MODULE = { path: '/mi-proyecto', label: 'Mi proyecto' }
