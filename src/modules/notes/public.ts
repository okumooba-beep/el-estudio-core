/**
 * Superficie pública del módulo Notas. Igual que Finanzas/Auditoría:
 * `public.ts` solo expone la identidad de navegación — la ruta importa
 * `NotesScreen` directo de su propio archivo (ver App.tsx).
 */
export const MODULE = { path: '/notas', label: 'Notas' }
