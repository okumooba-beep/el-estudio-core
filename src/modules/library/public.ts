/**
 * Superficie pública del módulo Library: lo único importable desde
 * fuera. ModulePlaceholder se agrega acá (fase "la habitación es el
 * sistema operativo") para que los tabs sin implementación propia
 * (Finanzas, IA) lo consuman sin import directo al interior.
 */
export { ModulePlaceholder } from './ModulePlaceholder'
