/**
 * Superficie pública del módulo Finanzas (Sprint "Build V1"): mismo
 * patrón que frases — `finanzas` ya existía reservado como IdeaDestino
 * y FurnitureId (ver work-table/destinoFurniture.ts) pero no tenía
 * ruta ni pantalla propia. La pantalla sigue siendo un
 * `ModulePlaceholder` genérico — esta identidad de navegación es real
 * independientemente de eso.
 */
export const MODULE = { path: '/finanzas', label: 'Finanzas' }
