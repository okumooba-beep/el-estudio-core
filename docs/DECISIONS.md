# DECISIONS

Registro liviano de decisiones arquitectónicas. Formato fijo: Fecha / Decisión / Motivo / Impacto. Nunca reemplaza a `EL_ESTUDIO_BIBLE.md` ni a `EL_ESTUDIO_CORE.md` — ambos siguen siendo la autoridad permanente; esto solo encadena el porqué de cambios puntuales de código.

---

**Fecha:** 2026-07-23
**Decisión:** El grafo de Graphify (`graphify-out/graph.json`) se regeneró de forma incremental (`graphify update . --no-cluster`, solo AST, sin LLM) antes de tocar cualquier código.
**Motivo:** el grafo existente todavía apuntaba a rutas de un layout anterior (`src/features/idea/...`) que ya no existe — el código real vive en `src/modules/work-table/...`. Consultarlo tal cual habría dado ubicaciones falsas y roto la regla de ahorro de tokens en vez de cumplirla.
**Impacto:** grafo actualizado a 1065 nodos / 1292 aristas, rutas correctas confirmadas (`rules.ts`, `RuleBasedClassifier.ts`, `RuleBasedClassifier.test.ts`). Ningún archivo de producto tocado por esta acción.

---

**Fecha:** 2026-07-23
**Decisión:** Se agregaron reglas de palabra clave para los destinos `finanzas`, `biblioteca` y `archivo` en `src/packages/cognitive-engine/providers/rule-based/rules.ts` (`presupuesto`/`factura`/`ahorro` → finanzas, `frase` → biblioteca, `archivar` → archivo).
**Motivo:** el Motor de Comprensión (Threshold, Prioridad de Módulo #2) solo reconocía 3 de los 7 destinos posibles (`habitos`, `trading`, `misiones`); el resto siempre caía en `hoy` sin propuesta. `DESTINO_TO_FURNITURE`/`FURNITURE_TO_DESTINO` ya tenían el mueble correspondiente definido para los tres destinos nuevos — el hueco era solo de reconocimiento, no de arquitectura. Es el cambio de mayor impacto y menor complejidad disponible sobre Threshold (Regla 13, 80/20) sin tocar UI, room ni cámara.
**Impacto:** archivo único modificado, aditivo, sin romper reglas existentes (orden de coincidencia sin colisiones nuevas verificado a mano). `npm run verify` (build + boundaries + lint + test) pasa limpio, 5/5 tests. Radio de impacto confirmado por Graphify: solo `RuleBasedClassifier.ts`, `log.ts`, `memory.ts` y su test dependen de este archivo.

---

**Fecha:** 2026-07-23
**Decisión:** Sprint de investigación de arquitectura de navegación — sin cambios de código. Se confirma que El Estudio tiene **tres capas de navegación reales y distintas**, no una duplicación accidental: (1) AppShell (sidebar/bottom-tabs, rutas planas: hoy, misiones, hábitos, trading, frases), (2) WorkspaceTabs (monitores del Escritorio, solo activa dentro de esa estación: today/trading/finanzas/ia/proyectos), (3) Room Modules (`roomModules.tsx`, por estación de cámara: archivador→JournalModule, pizarra→PlanningBoard, corcho→LibraryModule). Las capas (2) y (3) reutilizan las mismas pantallas que (1) (`TradingScreen`, `MisionesScreen`, `HabitosScreen`, `DiarioScreen`) en vez de duplicar lógica — esto es reutilización correcta, no deuda.
**Motivo:** el usuario pidió confirmar si "Trading alcanzable por múltiples superficies" era un patrón real, intencional, temporal o deuda, antes de tocar Today/Threshold. Se usó Graphify (`graphify update . --no-cluster` ya corrido el sprint anterior, reutilizado sin regenerar — sin cambios estructurales desde entonces) más un grep puntual que reveló un hueco de extracción del grafo en imports dinámicos (`lazy(() => import(...))` no genera arista).
**Impacto:** ningún archivo de producto modificado. Se detectaron dos inconsistencias reales que quedan documentadas, no resueltas unilateralmente: (a) `Libreta.tsx` todavía usa `navigate('/diario')` (salto de ruta plano) en vez de enfocar la estación `archivador` con la cámara — comportamiento anterior al sistema Room Modules, nunca migrado; (b) la estación `corcho` en `stations.ts` está etiquetada `"Archivo"` pero `LibraryModule.tsx` (su contenido real, `data-mueble={MUEBLES.biblioteca}`) se presenta como `"Biblioteca"` — el usuario que llega ve un nombre distinto al que lo trajo. Ninguna de las dos se corrigió porque requieren una decisión de producto (qué nombre gana, y si migrar el gesto de Libreta es seguro) que no corresponde asumir en este sprint.

---

**Fecha:** 2026-07-24
**Decisión:** `MisionPrincipal.tsx` (sección de Hoy) dejó de mostrar texto fijo ("Todavía no hay una misión principal") y ahora computa la misión principal real: la misión pendiente (`destino === 'misiones' && estado !== 'terminada'`) que más tiempo lleva esperando, ordenando por `createdAt` ascendente. Tocarla navega a `/misiones` (mismo gesto que `Libreta.tsx` usa para el Diario) en vez de duplicar ahí los controles de editar/marcar terminada que ya existen en `MisionesScreen.tsx`.
**Motivo:** era el único contenido de Hoy que seguía siendo un placeholder fijo — nunca leía datos reales, contradiciendo el objetivo del sprint ("Hoy debe comunicar qué merece atención"). Se decidió NO inventar un campo de prioridad nuevo (`estado`/`createdAt` ya alcanzan, y el Tablero de Misiones ya evita explícitamente badges/scores) — "lo que espera hace más tiempo" es el único criterio, sin colores ni métricas.
**Impacto:** un archivo modificado, reutiliza `useIdeas` (ya importado igual en `PhraseSlot.tsx`, mismo módulo) y `describeDay` (ya usado por Misiones/Hábitos/Trading) — cero lógica nueva, cero dependencias nuevas. `npm run verify` (build + boundaries + lint + test) pasa limpio. Sin cambios de arquitectura ni de navegación.

---

**Fecha:** 2026-07-24
**Decisión:** El registro semanal de Hábitos (`HabitosScreen.tsx`) ahora marca visualmente la columna del día de hoy (`habito-dia-hoy`, fondo `--accent-soft`) y da feedback táctil real al marcar un círculo (`:active` con `scale(0.92)`, el círculo marcado crece levemente) — antes las siete columnas (L-M-X-J-V-S-D) eran visualmente idénticas sin importar qué día era, y tocar un círculo no daba ninguna respuesta más allá del cambio de color ya existente en `.habito-dia-marcado`.
**Motivo:** tras inspeccionar `HabitosScreen.tsx`, `useHabitChecks.ts` y `habitCheckRepository.ts` (y, para comparar, `TradingScreen.tsx`) se confirmó que Hábitos ya es una función real y completa — persistencia Dexie genuina, sin datos falsos — así que no había ningún placeholder que reemplazar como en el sprint anterior con `MisionPrincipal`. El hueco real era más chico: la semana no comunicaba "hoy" (Regla de diseño del sprint: "todo detalle importa... estados de feedback, micro-interacciones"). No es un badge ni un score de prioridad (eso seguiría prohibido) — es orientación temporal pura, igual función que un calendario marcando el día actual.
**Impacto:** dos archivos modificados (`HabitosScreen.tsx`, `index.css`), ambos aditivos. Reutiliza el token `--accent-soft` que ya existía en el sistema de diseño (antes solo usado en el outline de foco) — ningún color nuevo. Se agregó el bloque `@media (prefers-reduced-motion: reduce)` correspondiente, siguiendo el mismo patrón que ya usan `.scene-camera` y otros componentes animados del archivo. Radio de impacto confirmado por Graphify: solo `src/modules/habits/public.ts` importa este componente. `npm run verify` (build + boundaries + lint + test) pasa limpio, 5/5 tests.

---

**Fecha:** 2026-07-24
**Decisión:** `AppShell.tsx` (barra de pestañas inferior en mobile + sidebar en desktop, la única superficie de navegación presente en cada pantalla del producto) ahora da feedback táctil real al tocar: `active:scale-90` en las pestañas mobile, `active:opacity-70` en los links del sidebar desktop, `transition-colors` agregado a la barra inferior (el sidebar ya lo tenía, la barra mobile no — inconsistencia entre las dos superficies), y las pestañas mobile pasan a tener `min-h-11` (44px) para cumplir el tamaño mínimo de touch target. Ambos con `motion-reduce:` para respetar el mismo principio de accesibilidad que ya aplican otros componentes vía `@media (prefers-reduced-motion: reduce)`.
**Motivo:** con Hoy, Threshold, navegación de cámara y Hábitos ya revisados en sprints anteriores, la auditoría "premium review" de este sprint buscó el problema de mayor impacto en consistencia — no en una pantalla aislada. La barra de navegación es la única interfaz que el usuario toca en absolutamente todas las sesiones, en cada transición, y no tenía ninguna respuesta visual inmediata al tocar (solo el cambio de color al llegar a la ruta activa, sin transición en mobile) — exactamente el tipo de brecha "se siente inconclusa" que el sprint pide resolver antes que agregar funciones nuevas.
**Impacto:** un archivo modificado (`AppShell.tsx`), sin CSS nuevo (Tailwind utilitario, consistente con cómo ya está escrito el resto del componente). Radio de impacto confirmado por Graphify (`affected` no reportó nodos — limitación conocida de imports dinámicos, ver hallazgo previo — cruzado a mano con grep: solo `App.tsx` lo monta). `npm run verify` (build + boundaries + lint + test) pasa limpio, 5/5 tests.

---

**Fecha:** 2026-07-24
**Decisión:** `useIdeas`, `useOperaciones` y `useHabitChecks` ahora exponen `ready: boolean` (falso hasta que la primera lectura a IndexedDB resuelve). Las cinco pantallas que deciden "está vacío" mirando `.length === 0` (`MisionPrincipal`, `HabitosScreen`, `MisionesScreen`, `TradingScreen`, `DiarioScreen`) ahora esperan a `ready` antes de mostrar ese mensaje — mientras carga, no muestran nada, igual que `PhraseSlot` ya hacía por diseño ("el silencio es el resultado más frecuente, no un caso de error").
**Motivo:** las cinco pantallas arrancan su estado en `[]` y recién lo llenan cuando la promesa de IndexedDB resuelve — antes de este cambio, cada montaje (abrir la app, o simplemente cambiar de pestaña y volver) mostraba un instante de "Ninguna misión está esperando" / "Ningún hábito existe todavía" incluso cuando sí había datos, porque `[] === todavía no llegó` y `[] === de verdad no hay nada` eran indistinguibles. Un usuario que guarda una idea y explora las pestañas en su primer minuto vería ese parpadeo de "no hay nada" en cada pantalla que visita — lo opuesto a "inmediato" y "predecible". No es cosmético: rompe la confianza de que el Estudio recuerda lo que se guardó.
**Impacto:** ocho archivos modificados (3 hooks + 5 pantallas), un único cambio de forma repetido de manera consistente — nunca un spinner nuevo ni una animación de carga: se reutilizó la misma filosofía de "silencio en vez de placeholder" que `PhraseSlot.tsx` ya tenía. Radio de impacto confirmado por Graphify (`affected` sobre los tres hooks) más grep manual sobre cada llamada a `useIdeas()` para no dejar ningún consumidor sin corregir. `npm run verify` (build + boundaries + lint + test) pasa limpio, 5/5 tests.

---

**Fecha:** 2026-07-24
**Decisión:** Los seis botones de acción restantes que todavía dependían solo de `hover:text-ink` para dar señal de "esto es interactivo" (`IdeaCapture.tsx` "Guardar", `MisionesScreen.tsx` "Nueva hoja", `HabitosScreen.tsx` "Nueva hoja", `TradingScreen.tsx` "Nueva operación" y "Editar plantilla", `MisionPrincipal.tsx` el link de la misión) ahora también responden a `active:` con el mismo color de destino que ya usaba el hover (`active:text-ink` / `group-active:text-ink`) — nunca un mecanismo nuevo (opacidad o escala), el mismo cambio de color que el botón ya prometía al pasar el mouse, ahora entregado también al tocar.
**Motivo:** `AppShell.tsx` (la barra de navegación) ya se había corregido en un sprint anterior, pero era una sola superficie; estos seis botones son las acciones "crear/guardar" repetidas en cada pantalla de datos del producto — la clase de interacción que más se toca por sesión después de navegar. En mobile (`hover` no existe) tocarlos no daba ninguna señal hasta que la acción ya había terminado, lo opuesto a "inmediato". Se eligió `active:text-ink` en vez de opacidad/escala (como en `AppShell`) porque acá el propio hover ya definía su color de "comprometido" (`text-ink`) — reusar ese mismo destino es más consistente que inventar un segundo lenguaje visual para el mismo botón.
**Impacto:** seis archivos modificados, un solo token de clase agregado por archivo, cero CSS nuevo, cero dependencias. Sin `motion-reduce` adicional: son transiciones de color puro (`transition-colors`), la misma categoría que el resto del código ya deja sin ese guard (solo se gatea `transform`/`scale`). `npm run verify` (build + boundaries + lint + test) pasa limpio, 5/5 tests.

---

**Fecha:** 2026-07-24
**Decisión:** `useIdeas().update`/`.moveSheet`, `useOperaciones().update` y `useHabitChecks().toggle` dejan de esperar a IndexedDB antes de actualizar la pantalla — el estado en memoria se actualiza en el mismo tick (con el patch ya conocido), y la escritura real corre en paralelo, ya no antes. `moveSheet.ts` pasa de una función async que persistía directamente a `buildMovePatch()`, una función pura que solo calcula el patch — la usan tanto el update optimista como la escritura de fondo, sin duplicar la lógica de destino/historial. `useHabitChecks().toggle` reconcilia el `id` real del check apenas persiste (antes de eso usa un id temporal), porque es el único caso donde el registro nuevo recién nace en la base.
**Motivo:** editar el texto de una hoja, marcar una misión terminada, archivar, tildar un ítem del checklist de un expediente y marcar un hábito del día son las acciones que más se repiten por sesión en todo el producto — y las tres implementaciones (`ideaRepository.update`, `operacionRepository.update`, `habitCheckRepository.setChecked`) comparten el mismo patrón: escriben en IndexedDB y después vuelven a leer (o buscar) antes de que la pantalla se actualizara, agregando una espera perceptible en la interacción más frecuente del Estudio. La sección "Perceived Performance" de este sprint lo dice de manera literal: si una operación no puede ser más rápida, tiene que sentirse más rápida — acá si puede ser más rápida además, porque la pantalla ya tiene todo lo que necesita para saber el resultado sin esperar la relectura.
**Impacto:** cinco archivos modificados (3 hooks + `moveSheet.ts` refactorizado a función pura + ningún cambio en los repositorios ni en su contrato público). Ninguna pantalla cambió su código de llamada: todas ya invocaban estas funciones sin depender de un valor resuelto salvo por el resultado ya reflejado en el estado, así que el fix es invisible a nivel de componente. Radio de impacto confirmado con Graphify (`affected` sobre los tres hooks y `moveSheet.ts`) — sin cambios estructurales, sin nuevas dependencias, no hace falta regenerar el grafo. `npm run verify` (build + boundaries + lint + test) pasa limpio, 5/5 tests. Queda pendiente el mismo tratamiento para `add()` en los tres hooks (crear una hoja/operación/hábito nuevo) — se dejó fuera a propósito porque ocurre con mucha menos frecuencia que editar/tildar, y closes over un id generado dentro del repositorio que la pantalla necesita de vuelta.

---

**Fecha:** 2026-07-24
**Decisión:** (Decisión de arquitectura mayor — nueva fase de producto, "Build V1"). `HoyScreen.tsx` deja de ser la escena de cámara (`useCameraRig`/`SceneStage`/`WorkspaceTabs`/`RoomObjects`/`MemoryLayer`) y pasa a ser una pantalla plana de una sola columna: `HoyHeader` → `PhraseSlot` → `IdeaCapture` → `MisionPrincipal` → `ModuleGrid` (nuevo). `ModuleGrid` es una lista con divisores fino (sin tarjetas, sin grid de widgets) que linkea a Diario, Misiones, Hábitos, Trading, Finanzas y Biblioteca. Se agregó `MODULE` a `journal/public.ts` (tenía ruta pero nunca identidad de navegación) y un módulo mínimo nuevo `finance/public.ts` + ruta `/finanzas` (mismo patrón exacto que `frases`/`ModulePlaceholder`, reutilizando el destino `finanzas` que `destinoFurniture.ts` ya reservaba). El fondo ambiental (`RoomBackground.tsx`, montado en `App.tsx`) no se tocó — la atmósfera se conserva, solo la capa de interfaz sobre ella cambió.
**Motivo:** instrucción explícita y directa del dueño del proyecto ("Build V1"): "Esto es una nueva fase de producto. Dejar de mejorar LifeOS. Empezar a construir El Estudio... el Home actual es un prototipo obsoleto, no preservar su layout ni su modelo de interacción." Este brief pisa a propósito, y solo para el Home, dos reglas permanentes de `EL_ESTUDIO_BIBLE.md`: el axioma "no es una aplicación, es un lugar" y la prohibición explícita del Cap. 11 de "dashboards nuevos". Se documenta acá porque el brief lo exige ("crear documentación solo si hay una decisión de arquitectura mayor") y porque revertir un axioma central sin dejar rastro escrito rompería la coherencia de `AI_DEVELOPMENT_RULES.md` para cualquier sprint futuro que lea el Home y no entienda por qué ya no respeta el axioma. La decisión de producto fue seguir la instrucción explícita del dueño tal cual, no cuestionarla — pero sí dejarla visible, nunca silenciosa.
**Impacto:** 5 archivos modificados (`HoyScreen.tsx` reescrito, `App.tsx`, `journal/public.ts`) + 2 archivos nuevos (`finance/public.ts`, `today/components/ModuleGrid.tsx`). `npm run verify` (build + boundaries + lint + test) pasa limpio, 124 módulos/205 dependencias cruzadas, 5/5 tests. El sistema de cámara/habitación (`features/room`, `features/workspace`, `features/memoria`, `components/room/RoomObjects.tsx` y sus dependencias en `packages/world/world/{stations,roomMemory}.ts`) queda sin ningún consumidor tras este cambio — no se borró en este sprint (Regla 12 de `AI_DEVELOPMENT_RULES.md`: alcance mínimo, reversible, un sprint no intenta completar la visión entera) y queda documentado como bloqueo pendiente para una decisión explícita de si se elimina o se reutiliza en la futura "Interfaz de Habitación" (Fase 4, `EL_ESTUDIO_CORE.md` Cap. 16). El índice de Graphify quedó desactualizado a propósito por esta misma acción: `graphify update` detectó 109 archivos con drift (la mayoría ajeno a este sprint, acumulado desde antes de Sprint 8) — regenerarlo en este sprint habría sido limpieza general fuera de alcance (Regla 3); queda como sugerencia, no como acción tomada.

---

**Fecha:** 2026-07-24
**Decisión:** (Decisión de arquitectura mayor — reconstrucción de producto, "Build Core V1", explícitamente NO un sprint incremental por instrucción directa del dueño: "This is not a sprint. Take the time required."). El Home se renombra puertas adentro a **El Estudio Core** y su interior se reconstruye por completo sobre la misma base de "Build V1" (una sola columna, sin cámara): `HoyHeader` pasa de mostrar "Hoy" fijo a un saludo según la hora real (Buenos días/tardes/noches) + fecha; `PhraseSlot` se integra al mismo bloque en vez de flotar centrado y en cursiva; `IdeaCapture` (Threshold) gana más aire, un placeholder y una línea inferior que se ilumina con `--accent` al enfocar — mismo formulario, cero lógica nueva; se agregan cuatro secciones nuevas — `ContinueWorking` (la Idea con `updatedAt` más reciente entre destinos reales), `HabitsGlance` (estado de HOY de cada hábito, nunca la grilla semanal completa), `RecentActivity` (lee `Idea.history`, que ya existía y nunca se leía desde afuera) — y `ModuleGrid` se reemplaza por `Spaces`, que agrega debajo de cada nombre el "Propósito" ya definido para ese módulo en `docs/EL_ESTUDIO_CORE.md` §5 (citado, no redactado de nuevo). Además, el sistema de cámara/habitación que "Build V1" había dejado como código muerto documentado (`features/room`, `features/workspace`, `features/memoria`, `components/room/{RoomObjects.tsx,roomObjectsRegistry.ts,objects/*}` y su lógica de envejecimiento en `lib/room/*` + `types/deskMemory.ts`, 20 archivos en total) se borra en este sprint. Por último, `index.css` ajusta la atmósfera de la habitación (ahora solo fondo, nunca interfaz): blur agregado a `.room-layer-lamp` (la ventana ya lo tenía) y una viñeta superior sutil (8%) en `#root::before` para que el saludo nunca compita con el brillo de la ventana.
**Motivo:** instrucción explícita y extensa del dueño del proyecto (brief "EL ESTUDIO — BUILD CORE V1"), que pisa a propósito dos cosas ya establecidas: (1) el ritmo de `AI_DEVELOPMENT_RULES.md` Reglas 12/13 (evolución incremental, sprints chicos) — el propio brief lo dice ("this is not a sprint... do not optimize for speed"); (2) la decisión de "Build V1" de dejar el código de cámara/habitación sin borrar como bloqueo reversible — este brief autoriza explícitamente lo contrario ("delete obsolete presentation code... never preserve something because it already exists"), y con `npx tsc -b` confirmando cero imports rotos tras el borrado, ya no había razón para seguir posponiéndolo. Se documenta acá por la misma razón que la entrada anterior: revertir una decisión ya escrita sin dejar rastro rompería la coherencia de este archivo para cualquier sprint futuro.

Una desviación deliberada del brief, no silenciosa: la lista sugerida de "Espacios" incluía "Planning" y "AI", ninguno de los dos con destino en `IdeaDestino` (`src/types/idea.ts`), mueble, ruta, ni fila propia en `EL_ESTUDIO_CORE.md` §5 — a diferencia de Finanzas (que ya tenía `destino`/`FurnitureId` reservados antes de "Build V1"), fabricar estos dos no tendría ningún dato ni pantalla real detrás. Se optó por mostrar los seis Espacios reales (Diario, Misiones, Hábitos, Trading, Finanzas, Biblioteca) y omitir los dos sin respaldo, siguiendo el mismo criterio que ya usa `muebles.ts` para no inventarle un mueble a Finanzas ("no corresponde inventarle uno sin que el Estudio lo pida").
**Impacto:** 4 archivos modificados (`HoyHeader.tsx`, `PhraseSlot.tsx` reescritos; `IdeaCapture.tsx` con cambios de clase únicamente, sin tocar sus handlers; `index.css`) + 2 archivos de módulo con una exportación nueva (`habits/public.ts` expone `useHabitChecks`, mismo patrón que ya usó "Build V1" para agregar `MODULE` a `journal/public.ts`) + 6 archivos nuevos en `today/components/` (`ContinueWorking.tsx`, `HabitsGlance.tsx`, `RecentActivity.tsx`, `Spaces.tsx`, `spaceRegistry.ts`) + `HoyScreen.tsx` reescrito para orquestarlos + `ModuleGrid.tsx` eliminado (reemplazado por `Spaces.tsx`). 20 archivos borrados del árbol de cámara/habitación (detalle arriba). `npm run verify` (build + boundaries + lint + test) pasa limpio: 108 módulos/181 dependencias cruzadas (baja desde 124/205 por el borrado, sin ninguna violación de `dependency-cruiser`), 5/5 tests. Nota técnica: el archivo de datos de Espacios no pudo llamarse `spaces.ts` por una colisión de mayúsculas/minúsculas de TypeScript en Windows contra `Spaces.tsx` (mismo nombre salvo case) — se renombró a `spaceRegistry.ts`. Tampoco pudo importar `@world/studio/muebles` directamente (regla `module-no-world` de `dependency-cruiser`, que solo exime a `IdeaCapture.tsx`/`MisionesScreen.tsx`/`HabitosScreen.tsx`/`TradingScreen.tsx`, no a un archivo nuevo) — se resolvió citando el "Propósito" ya documentado en `EL_ESTUDIO_CORE.md` §5 en vez de leer el grafo de muebles, sin tocar la configuración de arquitectura. El índice de Graphify sigue desactualizado (mismo hallazgo que la entrada anterior, ahora más lejos de la realidad del árbol tras este sprint) — se sugiere para un próximo sprint dedicado, no se ejecuta acá.

---

**Fecha:** 2026-07-25
**Decisión:** El proyecto queda con control de versiones y despliegue continuo por primera vez: `git init -b main`, remoto `origin` apuntando a `github.com/okumooba-beep/el-estudio-core`, primer commit (`ce0a6a8`) con todo el árbol existente, y proyecto Vercel (`nodo-venezuela/lifeos`) conectado al repo de GitHub — cada push a `main` dispara un build y despliegue de producción automático.
**Motivo:** hasta este sprint el código nunca había estado bajo control de versiones ni tenía una URL pública — instrucción explícita del dueño del proyecto de completar el pipeline de "código en la laptop" a "El Estudio accesible desde el teléfono". Se agregó `.venv-graphify/` (venv de Python de 169MB para graphify) al `.gitignore` existente antes del primer commit, porque no estaba cubierto y habría sido un error real de tamaño/portabilidad subirlo — el resto del `.gitignore` (Vercel lo extendió después con `.vercel`/`.env*`) se dejó intacto.
**Impacto:** ningún archivo de producto tocado. Esta misma entrada es, a propósito, el primer commit de prueba real del pipeline ya conectado: documenta y a la vez verifica que un push a `main` llega solo a producción sin ningún paso manual adicional.

**Fix (mismo día):** el primer push de prueba (`9af20e4`) quedó con estado `BLOCKED` en Vercel — la API de despliegues devolvió `readyStateReason: "The Deployment was blocked because GitHub could not associate the committer with a GitHub user."` GitHub no pudo asociar el email del commit (`nahuntrader33@gmail.com`) a la cuenta `okumooba-beep` porque esa cuenta no tiene email público (`GET /users/okumooba-beep` → `"email": null`, típico de "keep my email private" activado). Se corrigió configurando `user.email` local del repo (no global) al email noreply que GitHub genera para esa cuenta (`{id}+okumooba-beep@users.noreply.github.com`, id obtenido de la misma API pública) — el commit bloqueado no se reescribió (nunca se hace `--amend`/force-push sobre un commit ya empujado sin permiso explícito); el siguiente commit ya sale con el email correcto y dispara un nuevo despliegue.

---

**Fecha:** 2026-07-25
**Decisión:** (Pase de UX puro — "Core Experience V2", explícitamente NO un sprint de funcionalidad por instrucción directa del dueño: "THIS IS NOT A FEATURE SPRINT. THIS IS A UX RECONSTRUCTION."). Ningún hook, repositorio ni modelo de datos cambió; se ajustó presentación y copy en 7 archivos para crear jerarquía y calidez donde antes todo pesaba lo mismo: **Umbral** (`IdeaCapture.tsx`) gana tipografía más grande (17px→19px), `caret-accent`, más aire vertical (`pb-3`→`pb-4`, `py-3`→`py-3.5`) y una transición de foco más larga (300ms con `box-shadow` sutil en `--accent`) — pasa a ser el punto focal primario, sin tocar ningún handler. **Seguir con esto** (`ContinueWorking.tsx`) reemplaza el rótulo fijo "Seguir con esto" por una frase que depende del mismo dato de siempre (`describeDay` sobre `updatedAt`, ya usado por `RecentActivity`): "Hoy volviste a esto" / "Ayer estabas pensando en esto" / "Estabas en esto el {fecha}" — y su texto sube a 17px, el punto focal secundario. **Misión principal** cambia su eyebrow a "Lo que más importa hoy" y su meta-línea a "Es lo que espera hace más tiempo — desde {día}", explicitando la razón (antigüedad real de `createdAt`) en vez de solo reportar un estado. **Hábitos de hoy** (`HabitsGlance.tsx`) deja de ser un solo botón que navega entero: cada punto ahora llama directamente a `useHabitChecks().toggle(habitId, hoyISO, !marcado)` (mismo hook, mismo patrón optimista que ya usa `HabitosScreen.tsx`), y el nombre del hábito sigue navegando al Tablero por separado; cuando los tres se completan aparece "Ya hiciste lo que tenías que hacer hoy." — ninguna racha, badge ni puntaje. **Memoria** (antes "Actividad reciente", `RecentActivity.tsx`) cambia el verbo de bitácora ("se movió a" / "llegó a") por uno de recuerdo personal ("Lo llevaste a" / "Lo empezaste en"). **Espacios** (`spaceRegistry.ts`) reemplaza la columna "Propósito" citada de `EL_ESTUDIO_CORE.md` por las frases de personalidad que el propio brief de este sprint dio palabra por palabra para cada módulo (Diario "Pensá con claridad.", Misiones "Lo que merece acción." — la única fila que antes no tenía propósito —, Hábitos "Un día a la vez.", Trading "Operá con disciplina.", Finanzas "Entendé tu dinero.", Biblioteca "Todo lo que aprendiste."), y su chevron (`Spaces.tsx`) gana un desplazamiento de 2px al tocar (`group-active:translate-x-0.5`) para confirmar la navegación. **`HoyScreen.tsx`** reagrupa las 6 secciones en tres bloques con distinto aire entre sí en vez de un solo `gap-8` plano: el grupo "hoy" (saludo + voz + Umbral + Seguir con esto) respira con `gap-6` porque es un solo momento; el grupo de apoyo (Misión, Hábitos, Memoria) queda sin gap propio, apoyándose solo en el `pb-6` ya existente de cada sección, porque son apoyo, no protagonistas; Espacios cierra separado por `gap-10` porque es la salida del flujo, no parte de él — se quitó el `pb-6` redundante de `ContinueWorking.tsx` (quedaba duplicado contra el nuevo `gap-6` del padre). Por último, `HabitsGlance.tsx` necesitó una clase CSS nueva en `index.css` (`.habito-punto`/`.habito-punto-marcado`), mismo par color/transform que ya usa `.habito-dia`/`.habito-circulo` en `HabitosScreen.tsx`, con el mismo bloque `@media (prefers-reduced-motion: reduce)` que anula transform en toda la app.
**Motivo:** instrucción explícita y extensa del dueño del proyecto (brief "EL ESTUDIO — CORE EXPERIENCE V2"): "El Core ya funciona. Ahora tiene que volverse memorable... El objetivo ya no es la funcionalidad. El objetivo es la calidad emocional." El brief prohíbe explícitamente nuevos módulos, IA, gamificación y placeholders, y pide comparar cada decisión contra Things 3/Craft/Arc/Apple Notes/Notion Calendar "no visualmente, por oficio" — por eso ningún cambio agrega un dato o una interacción nueva de negocio: todos reutilizan un campo, un hook o una convención de CSS que ya existía (regla de reutilización de `AI_DEVELOPMENT_RULES.md` §4), y las frases de "Espacios" no se inventaron acá — son las que el propio brief entregó palabra por palabra. La jerarquía tipográfica (Umbral 19px > Seguir con esto 17px > el resto 15px) responde punto por punto al pedido de "un punto focal claro, uno secundario, todo lo demás como apoyo".
**Impacto:** 8 archivos modificados (`IdeaCapture.tsx`, `ContinueWorking.tsx`, `MisionPrincipal.tsx`, `HabitsGlance.tsx`, `RecentActivity.tsx`, `spaceRegistry.ts`, `Spaces.tsx`, `HoyScreen.tsx`) + `index.css` (una clase nueva, `.habito-punto`, con su contraparte `prefers-reduced-motion`). Cero archivos nuevos, cero componentes nuevos, cero dependencias nuevas — el pase completo es CSS/JSX de presentación y copy sobre componentes ya existentes. `npm run verify` (build + boundaries + lint + test) debe confirmarse limpio antes de este commit.

---

**Fecha:** 2026-07-27
**Decisión:** (Pase de reactividad y fricción — "Core Experience V3", explícitamente NO un sprint de funcionalidad: "THE CORE SHOULD STOP REPORTING. THE CORE SHOULD START RESPONDING."). El cambio de raíz es `useIdeas.ts`: pasa de un hook con estado local por instancia (cada componente que lo llamaba tenía su propia copia, leída una sola vez de IndexedDB al montar) a un store singleton a nivel de módulo con `useSyncExternalStore` (ya disponible en React 19, cero dependencias nuevas) — ahora cualquier `add`/`update`/`moveSheet`, sin importar qué componente lo dispare, notifica a todas las instancias montadas en el mismo tick. Es la razón de fondo por la que escribir una idea en el Umbral podía tardar una navegación completa (salir de Hoy y volver a entrar) en reflejarse en "Seguir con esto" o en Misión Principal — ya no. Sobre esa base, tres frentes de fricción concretos: (1) **Umbral** (`IdeaCapture.tsx`): tocar "Guardar" (a diferencia de Enter) le sacaba el foco al input — escribir la siguiente idea pedía un segundo toque; ahora un `ref` devuelve el foco al input apenas se guarda, y el botón "Guardar" iguala el `py-3.5` del input para el mismo target táctil. (2) **Seguir con esto / Misión principal — quitar duplicación**: `selectContinueWorking(ideas)` se extrae como función pura exportada de `ContinueWorking.tsx` (antes vivía adentro de un `useMemo` local); `HoyScreen.tsx` la llama una sola vez y pasa el resultado como prop `activa` a `ContinueWorking` y como `excludeId` a `MisionPrincipal`, para que la misma Idea nunca aparezca repetida en dos secciones seguidas cuando resulta ser, a la vez, la más reciente y la única misión pendiente. `MisionPrincipal` distingue ahora dos casos de "sin misión principal": cero pendientes en absoluto (mismo estado vacío de siempre) versus la única pendiente ya visible arriba (no muestra nada — nunca un "no hay nada" cuando sí hay algo, solo que ya se contó). (3) **Mobile — touch target**: `.habito-punto` (agregado en V2) medía 28px, debajo del mínimo táctil recomendado (~44px); crece a `2.75rem` sin tocar el tamaño del símbolo (`font-size` sin cambios). Se revisó explícitamente si hacía falta tocar safe-areas/scroll/teclado (`AppShell.tsx` ya cubre `env(safe-area-inset-*)` en las tres franjas y reserva `pb-28` para no quedar tapado por la barra inferior) y `RecentActivity.tsx`/`HabitsGlance.tsx`/`Spaces.tsx` (¿hay duplicación con algún módulo?) — en ambos casos no se encontró una brecha real, así que no se fabricó un cambio.
**Motivo:** instrucción explícita del dueño del proyecto (brief "EL ESTUDIO — CORE EXPERIENCE V3"): "The user should feel: 'I changed something.' not 'I have to refresh my mental model.'" El brief prohíbe explícitamente IA, módulos nuevos, dashboards y más navegación, y pide preferir sustracción sobre adición — por eso el cambio de mayor alcance (`useIdeas.ts`) no agrega ningún dato ni concepto de negocio nuevo: es exclusivamente la primitiva de React pensada para este problema exacto (estado compartido entre instancias), sin store global tipo Redux/Zustand. Se investigó y se descartó a propósito reusar `EventBus`/`AppEvents.ts` (infraestructura ya existente en `shared-kernel`) porque esa capa no puede depender del tipo `Idea` (regla de capas del proyecto) y porque el problema real — mismo hook, múltiples instancias — se resuelve más simple adentro del propio hook, sin un bus de eventos transversal a toda la app.
**Impacto:** 5 archivos modificados (`useIdeas.ts` reescrito, `IdeaCapture.tsx`, `ContinueWorking.tsx`, `MisionPrincipal.tsx`, `HoyScreen.tsx`) + `index.css` (un solo valor de tamaño ajustado, sin clases nuevas). Cero archivos nuevos, cero dependencias nuevas, API pública de `useIdeas()` sin cambios (`{ideas, ready, add, moveSheet, update}`) — ningún otro consumidor del hook en el árbol necesitó tocarse. `npm run verify` (build + boundaries + lint + test) pasa limpio: 108 módulos/180 dependencias cruzadas, 5/5 tests. `oxlint` deja una advertencia (no error) en `ContinueWorking.tsx` por exportar una función junto al componente en el mismo archivo (rompe Fast Refresh en dev) — se dejó así a propósito: separarla en un archivo nuevo solo para silenciar una advertencia de dev-experience habría ido en contra de "preferir sustracción sobre adición" y de la regla de mínimo número de archivos.

---

**Fecha:** 2026-07-27
**Decisión:** (Decisión de arquitectura mayor — "Threshold Experience V1", explícitamente la base de El Estudio y no un sprint de módulo: "THIS IS NOT A MODULE SPRINT. THIS IS THE FOUNDATION OF EL ESTUDIO."). Cinco frentes, ninguno con IA nueva. **(1) Umbral — clasificación pre-guardado**: `IdeaCapture.tsx` gana un segundo mecanismo de vista previa, distinto del que ya existía. El chip post-guardado ("Creo que esto pertenece a X", con corrección) sigue intacto y sin tocar; se agrega un `useMemo` que llama a `comprehensionEngine.classify(texto)` en cada tecla (la misma regla pura y barata que ya usa el guardado real, sin debounce porque no lo necesita) y muestra un mensaje de una línea antes de guardar (`PRECLASIFICACION_MESSAGE`), incluyendo el caso `destino === 'hoy'` ("Se queda como Idea.") — a diferencia del chip post-guardado, que nunca opina sobre `hoy` porque ahí no hay corrección que ofrecer, esta vista previa sí lo reporta porque es un espejo transparente del estado real del clasificador, no una sugerencia. Guardar también gana una confirmación visual: 900ms de un estado `justSaved` que reutiliza el mismo borde/sombra de foco que ya existía, ninguna animación nueva. **(2) Diario — reconstrucción completa**: `DiarioScreen.tsx` se reescribe entero (Regla explícita del brief: "destroy the current implementation if necessary"). Deja de ser una grilla de post-its (`.idea-hoja`/`.material-paper`, compartida con Misiones/Hábitos/Trading) y pasa a ser una sola columna de entradas (`destino === 'hoy'` únicamente, sin misiones/hábitos/trading mezclados) con clases nuevas y deliberadamente sin relación con las anteriores — `.diario-*`, no `.journal-*` (ver hallazgo abajo) — para que se sienta "abrir un cuaderno real" en vez de una mesa de trabajo con notas sueltas. **(3) Misión desde el Core**: `MisionPrincipal.tsx` gana un botón `.habito-punto` (mismo círculo ○/● que ya usa `HabitsGlance.tsx` para "completar sin salir del Core") que llama `update(principal.id, { estado: 'terminada' })` sobre el mismo `useIdeas()` ya singleton (V3) — cero mecanismo nuevo, la actualización ya es instantánea porque el store lo es desde V3. **(4) Finanzas — primera versión real**: se agrega el primer modelo de datos nuevo del sprint. `types/finance.ts` (`FinanceAccount` con `tipo: 'liquidez'|'inversion'|'deuda'` — una sola tabla de cuentas sirve Patrimonio Neto/Liquidez/Inversión/Deuda agrupando por `tipo`, en vez de cuatro tablas paralelas; `FinanceMovimiento`, ledger de solo-alta para Flujo de Caja; `FinanceGoal` para Metas), `db.ts` sube a `version(8)` agregando las tres tablas sin tocar ninguna anterior, `financeRepository.ts` (mismo patrón `Dexie*Repository` que `operacionRepository`/`habitCheckRepository`), `useFinance.ts` (hook `useState`+`useEffect` simple, no el store singleton de `useIdeas` — Finanzas no tiene todavía un segundo consumidor que justifique esa complejidad; se documenta como decisión reversible, no como omisión) y `FinanceScreen.tsx`, ruteado directo en `App.tsx` (mismo patrón de import directo que `HoyScreen`/`DiarioScreen`, nunca vía `public.ts`) reemplazando el `ModulePlaceholder` que ocupaba `/finanzas`. Sin gráficos ni barras — números en texto plano, foundations only, tal como pide el brief. **(5) Frases — sistema de Citas propio**: se llena el stub de `getSabiduria()` en `voiceEngine.ts` (existía desde Sprint 3.3, siempre devolvía `null`) con un motor nuevo (`packages/world/quotes/{quotes.ts,quoteEngine.ts}`) deliberadamente separado del Manifiesto (`phraseEngine.ts`/`phrases.ts`, 50 frases genéricas del Estudio con gate de silencio de 4 días) porque son fuentes distintas: Citas es la colección curada de frases propias del usuario, sin gate de silencio, rotando cada 4 horas (`BUCKET_HOURS`) con la misma técnica de no-repetición que ya usa `phraseEngine` (`pickIndex`); `QUOTES` arranca vacío a propósito (cero frases fabricadas) con un comentario mostrando el formato exacto para pegar las frases reales del usuario después, sin tocar ningún otro archivo. **Atmósfera**: dos ajustes de contraste sobre los mismos dos levers que ya existían desde sprints anteriores, ningún mecanismo nuevo — `.room-layer-window` baja su multiplicador de opacidad de `0.55` a `0.45`, y `#root::before` (la viñeta superior/inferior) profundiza de `0.16/0.32` a `0.20/0.38`, preservando intacta la banda central transparente (`transparent 18%` a `transparent 62%`) donde vive el contenido — la habitación sostiene la interfaz, nunca compite con ella.
**Hallazgo, no arreglado (fuera de alcance, Regla 3):** al escribir las clases nuevas del Diario se confirmó que el bloque `.journal-*` de `index.css` (`.journal-stage`/`.journal-shadow`/`.journal-book`/`.journal-pages` + sus keyframes) es código muerto de una coreografía 3D de "libro abriéndose" atada a un `SceneStage`/sistema de cámara que ya no existe (eliminado en Build Core V1) — más grande de lo que sprints anteriores habían registrado. No se tocó en este sprint (limpieza general fuera de alcance); se nombraron las clases nuevas `.diario-*` específicamente para no colisionar ni confundirse con él. Queda sugerido para un sprint de limpieza dedicado.
**Motivo:** instrucción explícita y extensa del dueño del proyecto (brief "EL ESTUDIO — THRESHOLD EXPERIENCE V1"): "Think about the first place where every idea in a person's life arrives. That place is the Threshold... Transform Threshold from a text input into the heart of El Estudio." El brief prohíbe explícitamente construir IA nueva ("DO NOT BUILD AI. Reuse the existing classification rules.") y fija la regla de producto del sprint ("Every implemented change must satisfy at least one: (1) Reduce real friction, or (2) Increase the desire to open El Estudio tomorrow") — por eso ningún frente de este sprint agrega un motor de decisiones nuevo: la vista previa de clasificación reutiliza literalmente la misma llamada (`comprehensionEngine.classify`) que el guardado real ya hacía: nunca finge inteligencia donde no la hay. La reconstrucción del Diario responde a la instrucción literal del brief ("Completely rebuild the Journal... Destroy the current implementation if necessary... No Missions. No Habits. No Trading. Only thoughts"). Finanzas responde a "Start building the first real version. Do NOT create a spreadsheet" con "Build foundations only" — de ahí que el modelo de datos elija una tabla de cuentas agrupada por `tipo` en vez de cuatro tablas: es la versión mínima que ya soporta las 4 métricas pedidas sin inventar estructura que el brief no pidió todavía. El sistema de Citas responde punto por punto a los requisitos explícitos del brief ("local data source; no repetition during short periods; support manual expansion; change according to time of day") y a la instrucción de que las frases curadas reales del usuario se puedan agregar después "without changing the code structure" — de ahí el archivo `quotes.ts` separado, vacío, con el formato documentado en un comentario.
**Impacto:** 8 archivos modificados (`IdeaCapture.tsx`, `MisionPrincipal.tsx`, `voiceEngine.ts`, `db.ts`, `finance/public.ts`, `App.tsx`, `index.css` con dos ediciones distintas) + 1 archivo reescrito entero (`DiarioScreen.tsx`) + 7 archivos nuevos (`types/finance.ts`, `finance/financeRepository.ts`, `finance/useFinance.ts`, `finance/FinanceScreen.tsx`, `world/quotes/quotes.ts`, `world/quotes/quoteEngine.ts`). `npm run verify` (build + boundaries + lint + test) pasa limpio: 114 módulos/193 dependencias cruzadas, sin violaciones de `dependency-cruiser`, 5/5 tests. Único hallazgo de `oxlint`: la misma advertencia pre-existente de Fast Refresh en `ContinueWorking.tsx` documentada en la entrada de V3 — no relacionada con este sprint, no tocada.

---

**Fecha:** 2026-07-27
**Decisión:** (Pase de voz y copy — "The Voice of El Estudio", explícitamente NO un sprint de arquitectura ni de rediseño: "Reuse the existing architecture... Do NOT redesign existing modules."). Se auditó cada texto visible del producto (encabezados, estados vacíos, botones, mensajes, transiciones, placeholders, confirmaciones, errores, descripciones) módulo por módulo; la mayoría ya estaba alineada con la voz pedida (corta, cálida, sin explicar de más — confirmado en `voiceEngine.ts`, que ya contiene literalmente "Hoy el Estudio está en silencio.", el propio ejemplo del brief) así que el trabajo real fueron correcciones puntuales, no una reescritura general. **(1) Nombre de marca**: se encontró la inconsistencia de copy más significativa del producto — "LifeOS" seguía visible en el wordmark del sidebar (`AppShell.tsx`), el `<title>`, el meta `description`, el `apple-mobile-web-app-title` (lo que iOS muestra bajo el ícono tras "Agregar a inicio" — impacto directo en el phone test) y el manifest PWA (`vite.config.ts`, `name`/`short_name`/`description`) pese a que el producto se llama "El Estudio" desde hace varios sprints. Los cinco puntos pasan a decir "El Estudio". Deliberadamente NO tocados: `db.ts` (`super('lifeos')`, nombre real de la base IndexedDB — cambiarlo apuntaría la app a una base nueva y vacía, pérdida de datos aparente) y `package.json` (`"name": "lifeos"`, identificador interno de npm, nunca visible al usuario, fuera de alcance por Regla 3). **(2) Umbral — clasificación**: `IdeaCapture.tsx` unifica dos mapas de copy que decían cosas distintas para el mismo dato (`PRECLASIFICACION_MESSAGE` antes de guardar vs. `DESTINO_LABEL` embebido en "Creo que esto pertenece a X" después de guardar) en uno solo, `DESTINO_PREVIEW_MESSAGE`, usado en ambos momentos. La propuesta post-guardado deja de hacer del nombre del destino el botón (nada indicaba que esa palabra fuera tocable); ahora un botón separado y explícito, "Cambiar destino" (mismo `.idea-proposal-target` ya existente, mismo handler), seguido del ejemplo literal del brief ("Parece una misión." Cambiar destino). Se agrega "Guardado." como confirmación de guardado reutilizando el `justSaved` que ya existía (Threshold V1) — cuando `justSaved` es true se muestra "Guardado." en el mismo slot de una línea que normalmente muestra la vista previa de clasificación; ningún estado, timer ni componente nuevo. **(3) Continuar trabajando**: `fraseContinuar()` (que reportaba fecha: "Hoy volviste a esto"/"Ayer estabas pensando en esto"/"Estabas en esto {día}") se elimina junto con el import de `describeDay`, ya sin otro uso en el archivo; la etiqueta pasa a ser la invitación fija "¿Continuamos?" que el brief pide literalmente en vez del reporte. `selectContinueWorking` (la selección pura) no cambia. **(4) Diario**: la descripción del estado vacío decía "Lo que escribas en el Umbral y se quede como Idea va a quedar acá." — "Umbral" e "Idea" son sustantivos internos del sistema que el usuario nunca ve como etiqueta real en ninguna pantalla; pasa a "Lo que escribas hoy va a quedar acá, como una página más.", sin lenguaje de módulo, tal como pide el brief ("No module language. Only writing."). **(5) Finanzas**: el título del estado vacío ("Todavía no hay nada acá.") rompía el patrón ya establecido en Misiones/Hábitos/Trading/Biblioteca ("Ningún/Ninguna {concepto} vive/existe todavía."); pasa a "Ninguna cuenta vive acá todavía." La descripción ya estaba bien, sin cambios. **(6) Citas**: se confirmó que `quoteEngine.ts`/`quotes.ts` (construidos en Threshold V1) ya satisfacen este brief sin ningún cambio de código — colección curada únicamente (`QUOTES` arranca vacío, cero frases fabricadas), rotación por franja horaria de 4 horas, no-repetición vía `pickIndex` (ninguna frase se repite antes de agotar el repertorio) y expansión futura documentada en el propio archivo. **(7) Visual**: se confirmó que los dos ajustes de contraste ya hechos en Threshold V1 (`.room-layer-window` opacidad ×0.45, viñeta de `#root::before` profundizada a 0.20/0.38) ya cubren el pedido repetido de "increase contrast, darken overlays" de este brief; no se agregó un tercer ajuste superpuesto para no oscurecer más allá de lo que el propio sprint anterior ya calibró contra la banda central transparente del contenido.
**Motivo:** instrucción explícita del dueño del proyecto (brief "EL ESTUDIO — THE VOICE OF EL ESTUDIO"): "Create one consistent voice across the entire product... Never generic. Never corporate. Never motivational" y la regla del sprint ("Every change must satisfy at least one: Reduce friction, or Increase the desire to open El Estudio tomorrow. Otherwise do not implement it."). Por eso el alcance se mantuvo estrictamente en texto (y, donde el propio texto lo exigía, el mínimo affordance de UI que lo sostiene, como el botón "Cambiar destino") — ningún módulo se rediseñó, ningún dato ni mecanismo de negocio nuevo se agregó, y Graphify no se regeneró porque ningún cambio estructural lo requería (regla explícita del brief: "Do NOT regenerate Graphify unless structural changes require it"). El hallazgo de "LifeOS" se investigó y resolvió con especial cuidado en los dos lugares donde tocarlo habría sido un cambio de identidad, no de copy (nombre real de la base de datos, identificador de npm) — se dejaron intactos a propósito, documentado acá para que un sprint futuro no los toque por error creyendo que es la misma clase de cambio.
**Impacto:** 6 archivos modificados (`AppShell.tsx`, `index.html`, `vite.config.ts`, `IdeaCapture.tsx`, `ContinueWorking.tsx`, `DiarioScreen.tsx`, `FinanceScreen.tsx` — 7 en total). Cero archivos nuevos, cero dependencias nuevas, cero cambios de esquema o de datos. `npm run verify` (build + boundaries + lint + test) pasa limpio: 114 módulos/192 dependencias cruzadas, sin violaciones de `dependency-cruiser`, 5/5 tests. Único hallazgo de `oxlint`: la misma advertencia pre-existente de Fast Refresh en `ContinueWorking.tsx` (exportar `selectContinueWorking` junto al componente) ya documentada en la entrada de V3 — no introducida ni agravada por este sprint.

---

**Fecha:** 2026-07-27
**Decisión:** (Rediseño completo del Diario — "PROJECT — THE NOTEBOOK", explícitamente un sprint de UX/producto, no de arquitectura: "Reuse current architecture... No new dependencies... Forget the current implementation. Build a place for thinking."). `DiarioScreen.tsx` se reescribe entero por segunda vez (la primera fue Threshold V1, que ya lo llevó de grilla de post-its a columna de lectura de solo consulta) — esta vez el cambio de fondo es que **escribir pasa a ser la interacción primaria**, no un formulario aparte: la pantalla abre directo en la página de hoy con un `<textarea>` de una fila (auto-crece con `scrollHeight`) ya listo para recibir texto, sin botón "Guardar" ni modal. El modelo de datos no cambia un bit: sigue siendo `Idea` con `destino === 'hoy'` (mismo filtro desde Threshold V1), agrupado en un `Map<fecha, Idea[]>` por `useMemo` — cada fecha con contenido es "una página", ordenada por `hora` dentro del día. **Guardado**: se reutiliza literalmente el patrón `.expediente-textarea` de `TradingScreen.tsx` (`onBlur` dispara la escritura, no un debounce mientras se tipea) — al perder el foco, si hay texto, `add(texto)` (mismo hook `useIdeas()` singleton de Core V3, sin opciones) crea un nuevo párrafo y limpia el borrador para el siguiente. Se agrega una red de seguridad que Trading no necesitaba (un formulario de expediente no se abandona a mitad de frase con la misma frecuencia que una página en blanco): un listener de `visibilitychange` más un flush al desmontar, ambos leyendo de un `borradorRef` (nunca tocan estado de React, porque pueden dispararse con el componente ya fuera de pantalla) para no perder un párrafo a medio escribir si el usuario cambia de pestaña o cierra la app. **La garantía estructural pedida por el brief** ("The Journal is NOT a task manager... No Mission language. No Trading language. No Finance language.") no se resuelve solo con texto: `DiarioScreen.tsx` nunca importa ni llama a `comprehensionEngine.classify()` — a diferencia del Umbral (`IdeaCapture.tsx`), que sí clasifica cada tecla para proponer un destino, el Diario es la única superficie del Estudio que no puede, ni por error, enrutar lo que se escribe hacia Misiones/Hábitos/Trading. **Tinta fresca vs. tinta seca**: el `<textarea>` activo se ve en `var(--ink)` (contraste completo) mientras los párrafos ya guardados quedan en `var(--ink-dim)` — distinción puramente tipográfica (ningún componente ni badge nuevo) entre "esto se está escribiendo ahora" y "esto ya quedó asentado". **Encabezado de página**: función local `fechaDePagina()`, deliberadamente no `describeDay()` (que comparten Misiones/Hábitos/Trading para una línea de evidencia mínima) porque su fallback crudo ("el 2026-07-20") no se siente como el título de una página de cuaderno real; usa `Intl.DateTimeFormat('es', {weekday:'long', day:'numeric', month:'long'})`, ya nativo del navegador, cero dependencia nueva. **Navegación**: "← Página anterior" / "Página siguiente →" como texto plano (mismo estilo `.expediente-faint` — sin fondo ni borde, solo color al pasar el mouse/tocar — que ya usa Trading), recorriendo una lista descendente de fechas con contenido más el día de hoy (siempre presente aunque esté en blanco); deliberadamente sin selector de calendario, sin paginación numerada, sin atajo "ir a hoy" — el brief pide "make old pages feel like opening previous notebook pages", no un buscador. **Estado vacío**: nunca "No hay entradas" — el propio `placeholder` del textarea dice "Empezá cuando quieras." (primera página) o "Seguí escribiendo…" (páginas con contenido), adaptado a la voz voseo ya establecida (`"¿Qué tenés en mente?"`) en vez de la frase literal del brief, que el brief mismo autoriza ("or equivalent"); así la superficie de escritura es, a la vez, el estado vacío — sin caja ni componente `EmptyState` separado. **Layout**: sin cards, sin dashboard — una sola columna centrada (`max-w-xl`), márgenes grandes, whitespace generoso; el confirmador "Guardado." (900ms, mismo patrón `justSaved` que Threshold V1) se renderiza condicionalmente para no dejar un hueco vertical permanente cuando no está activo. **Limpieza de código muerto**: se aprovecha esta reescritura — la misma pantalla, el mismo archivo de estilos — para borrar el bloque `.journal-stage`→`.journal-cover-spine` (~380 líneas de `index.css`, coreografía 3D de "libro abriéndose" atada a un `SceneStage` ya eliminado en Build Core V1, señalada como código muerto desde la entrada de Threshold V1 pero diferida entonces a "un sprint de limpieza dedicado") junto con sus dos referencias vestigiales en `ambientEvents.ts` (`'journal-page-settle-left/-right'` → `'journal_page_settle'`, un `AmbientEventId` sin listener en ningún lugar del árbol — confirmado por grep). No se tocó ningún otro bloque de `index.css` fuera de `.diario-*`/`.journal-*`.
**Motivo:** instrucción explícita del dueño del proyecto (brief "PROJECT — THE NOTEBOOK"): "The Journal is NOT a note app / a task manager / an inbox / a module browser. The Journal IS a notebook / reflection / long-form thinking / writing" y la regla del sprint ("Every change must: Reduce friction OR Increase desire to write. Otherwise don't implement it."). Por eso ningún mecanismo se inventó donde ya existía uno probado: el guardado reutiliza el patrón de blur de Trading en vez de un autosave con debounce nuevo, la selección de página reutiliza `Idea`/`destino: 'hoy'` sin tocar `ideaRepository.ts` ni el esquema de Dexie, y el hook de datos sigue siendo el mismo `useIdeas()` singleton de Core V3. La decisión de borrar el bloque `.journal-*` (explícitamente diferida en Threshold V1) se reconsideró acá porque el brief pide "forget the current implementation" sobre la misma pantalla y el mismo archivo de estilos que ese bloque muerto ocupa — dejar de lado una reescritura completa del subsistema no es el mismo tipo de alcance que un sprint de limpieza general no relacionado, así que se juzgó dentro de alcance esta vez, no una excepción a la Regla 3. Graphify no se regeneró: ningún módulo, tipo ni relación estructural cambió (regla explícita del brief: "Do not regenerate Graphify unless structure changed").
**Impacto:** 1 archivo reescrito entero (`DiarioScreen.tsx`), 2 archivos modificados (`index.css`: nuevas reglas `.diario-*` + borrado del bloque muerto `.journal-*`; `ambientEvents.ts`: remoción de las dos entradas muertas y el miembro `journal_page_settle`). Cero archivos nuevos, cero dependencias nuevas, cero cambios de esquema o de datos — la única corrección de tipos fue ajustar dos accesos a arreglo (`fechas[indice ± 1]`) con `?? fechaActual` y reemplazar un parseo manual de fecha por `new Date(\`${fecha}T00:00:00\`)`, ambos por `noUncheckedIndexedAccess` de TypeScript, sin cambiar ningún comportamiento. `npm run verify` (build + boundaries + lint + test) pasa limpio: 114 módulos/190 dependencias cruzadas, sin violaciones de `dependency-cruiser`, 5/5 tests. Único hallazgo de `oxlint`: la misma advertencia pre-existente de Fast Refresh en `ContinueWorking.tsx`, ya documentada en V3 y en la entrada de Voz — no relacionada con este sprint.

---

**Fecha:** 2026-07-27
**Decisión:** (Pase de comodidad de escritura — "Sprint: Writing Comfort", explícitamente NO un sprint de funciones: "Improve the writing experience. Do not add features. Increase comfort."). Tres frentes, todos sobre el Diario reescrito en "THE NOTEBOOK". **(1) Renombre**: "Diario" pasa a llamarse **"Cuaderno"** en las dos únicas superficies donde el usuario realmente lee esa palabra — `journal/public.ts` (`MODULE.label`, consumido por `Spaces.tsx` en la lista de Espacios de Hoy) y `IdeaCapture.tsx` (`CORRECCION_DESTINOS`, el botón "Cambiar destino" del Umbral). Se investigó cada ocurrencia de la palabra en `src/` antes de decidir el alcance: la ruta (`/diario`), el nombre de carpeta (`journal/`), el archivo (`DiarioScreen.tsx`), el prefijo de clases CSS (`.diario-*`) y el string interno `FurnitureId: 'diario'` quedan intactos — mismo precedente ya establecido en el propio código (`frases/public.ts` expone `path: '/frases'` con `label: 'Biblioteca'`, ruta y etiqueta ya divergen sin que nadie lo considere una inconsistencia). Dos ocurrencias más de "Diario" quedaron confirmadas como código muerto o metadato nunca renderizado (`world/stations.ts`, un array `STATIONS` sin ningún importador; `studio/furniture.ts`, el campo `.nombre` de `FURNITURE.diario`, nunca leído por ninguna pantalla) — no se tocaron, fuera de alcance de un renombre de UI. **(2) Legibilidad**: se midió a mano el contraste WCAG de cada token de color usado en `.diario-*` contra `--canvas` (luminancia relativa + fórmula de razón de contraste, sin herramienta): `--ink` da ~15.3:1, `--ink-dim` ~7.75:1 (ambos excelentes), pero `--ink-faint` — el que realmente usaban `.diario-fecha-pagina`, `.diario-escritura::placeholder`, `.diario-guardado` y `.diario-nav button` — da ~3.78:1, por debajo del mínimo AA (4.5:1). Es el mismo defecto que Threshold V1 ya había encontrado y corregido en los links del nav de `AppShell.tsx` cuando el resplandor de la ventana los lavaba a mediodía — `--ink-faint` queda establecido como un token fragil bajo la luz dinámica de la habitación. Las cuatro reglas pasan a `--ink-dim`, sin inventar ningún color nuevo. Se subió además `line-height` de 1.7 a 1.8 y `margin-bottom` de `.diario-parrafo` de 1.1rem a 1.6rem, en `.diario-parrafo` y `.diario-escritura` por igual, para que un párrafo guardado y el que se está escribiendo respiren lo mismo. **(3) Visual**: se borró `.diario-pagina` (y su `::before`, la línea vertical de acento que el propio comentario del código describía como "el margen de una libreta real") porque violaba literalmente la prohibición explícita del brief ("Do NOT simulate paper. Do NOT simulate a notebook.") — a diferencia de los otros cambios, éste no pasó por el filtro de la Regla del sprint (legibilidad o comodidad) porque el brief lo prohíbe de forma incondicional, no condicional. Se evaluó y se descartó a propósito un scrim/oscurecimiento del área de escritura, pese a que el brief lo autorizaba explícitamente ("If necessary, darken only the writing area"): con el resplandor de ventana en su peor caso (mediodía) el fondo efectivo detrás del texto sigue siendo lo bastante oscuro para que `--ink-dim` (7.75:1) e `--ink` (15.3:1) se mantengan muy por encima de AA — la única deficiencia real y medida era `--ink-faint`, ya resuelta sin agregar ningún color ni capa nueva; un scrim habría sido tanto innecesario como riesgoso, porque cualquier rectángulo/gradiente detrás del texto puede leerse como una "página" o "tarjeta" — exactamente lo que este mismo brief prohíbe. `DiarioScreen.tsx` pierde la clase `diario-pagina` del contenedor (ahora un `<div>` sin estilos propios) y el `px-1` de su `<header>` (ya no hace falta compensar el margen borrado). Por último, `autoFocus` en el `<textarea>` (solo se renderiza cuando `esHoy`) para que el cursor esté listo apenas se entra a la página de hoy, sin ningún mecanismo nuevo — se re-dispara cada vez que se vuelve a Hoy porque el textarea se desmonta/remonta con `esHoy`, comportamiento intencional, no un bug.
**Motivo:** instrucción explícita del dueño del proyecto (brief "EL ESTUDIO — SPRINT: WRITING COMFORT"): "Improve the writing experience. Do not add features. Increase comfort" y la regla del sprint ("Every change must satisfy: Better readability OR Better writing comfort. Otherwise do not implement.") — salvo la remoción del acento de margen, que el brief prohíbe de forma incondicional en su sección VISUAL, no sujeta a esa regla. El alcance del renombre se decidió citando un precedente ya existente en el propio código (`/frases`↔"Biblioteca") en vez de inventar un criterio nuevo. La decisión de no agregar un scrim se documenta acá con la matemática de contraste que la respalda, para que un sprint futuro no lo agregue por error creyendo que el brief lo pedía sin condición.
**Impacto:** 4 archivos modificados (`journal/public.ts`, `work-table/IdeaCapture.tsx` — una sola línea cada uno, el renombre —, `index.css` — reescritura del bloque `.diario-*`, sin agregar ni un color nuevo —, `DiarioScreen.tsx` — tres cambios de clase/atributo, ningún cambio de lógica). Cero archivos nuevos, cero dependencias nuevas, cero cambios de esquema o de datos, cero regeneración de Graphify (ningún cambio estructural: mismos módulos, mismas relaciones). `npm run verify` (build + boundaries + lint + test) pasa limpio: 114 módulos/190 dependencias cruzadas, sin violaciones de `dependency-cruiser`, 5/5 tests. Único hallazgo de `oxlint`: la misma advertencia pre-existente de Fast Refresh en `ContinueWorking.tsx`, ya documentada en sprints anteriores — no relacionada con este.

---

**Fecha:** 2026-07-27
**Decisión:** (Sprint de diseño puro — "THE SEED", explícitamente no de implementación: "Prepare ideas to evolve... Do not implement conversion yet. Only design the foundation."). **Cero archivos de `src/` modificados.** Tras auditar el modelo de datos completo de las cuatro evoluciones que el brief pide preparar (Misión, Hábito, Nota de Trading, Ítem de Finanzas), se concluyó que la base ya existe para dos de las cuatro y que agregar código para las otras dos sería especular sobre un mecanismo que todavía no se construye — así que el entregable de este sprint es el diseño en sí, no una migración de esquema.

**Hallazgo 1 — Misión y Hábito ya son evolucionables hoy, sin ningún cambio:** ambos son, literalmente, el mismo objeto `Idea` que vive en el Cuaderno (`destino: 'hoy'`), solo que con `destino: 'misiones'` o `destino: 'habitos'`. `buildMovePatch()` (`moveSheet.ts`) ya acepta cualquier `FurnitureId` y ya recalcula `destino`/`estado`/`history` para los siete destinos posibles (`DESTINO_TO_FURNITURE`, `destinoFurniture.ts`) — el mecanismo de "esta nota se convierte en otra cosa" ya está construido desde Sprint 3.6/Threshold V1, simplemente nunca se disparó todavía desde adentro del Cuaderno (que a propósito nunca corre `comprehensionEngine.classify()`, ver Sprint "THE NOTEBOOK"). `Idea.origen` y `Idea.history` ya preservan de dónde vino cada hoja. Cero fundamento nuevo hace falta acá.

**Hallazgo 2 — Trading y Finanzas son entidades completamente distintas de Idea, con un vacío real ya existente:** Trading es `Operacion` (`types/operacion.ts`) y Finanzas es `FinanceAccount`/`FinanceMovimiento`/`FinanceGoal` (`types/finance.ts`) — tablas y formas propias, sin ningún campo que apunte de vuelta a una `Idea`. Más importante: el clasificador (`rules.ts`) ya puede sugerir `destino: 'trading'`/`'finanzas'` para una idea, y el botón manual "Cambiar destino" de Threshold (`IdeaCapture.tsx`, `CORRECCION_DESTINOS`) ya ofrece "Trading" como destino hoy mismo — pero ni `TradingScreen.tsx` ni `FinanceScreen.tsx` leen jamás `Idea` (ambas leen exclusivamente `useOperaciones()`/`useFinance()`, sus propias tablas). El resultado: una idea movida a esos dos destinos desaparece silenciosamente, sin ninguna pantalla que vuelva a mostrarla. Esto no lo causó ni lo corrige este sprint — es un vacío pre-existente en la arquitectura de destinos, descubierto al analizarla para este brief, y se deja documentado como el hallazgo más importante, no resuelto (Regla 10 de `AI_DEVELOPMENT_RULES.md`: informar limitaciones y próximos pasos, nunca implementarlos en el mismo sprint).

**Diseño de la semilla (no implementado):** cuando una futura etapa construya la conversión real, el único cambio de esquema que hace falta es un puntero de procedencia opcional y nulo — `origenIdeaId: string | null` — agregado a `Operacion`, `FinanceAccount`, `FinanceMovimiento` y `FinanceGoal`. Nunca requerido, nunca poblado por ningún código de hoy, nunca leído por ninguna pantalla todavía: solo el gancho mínimo para que, el día que exista un botón real de "convertir", el registro nuevo pueda decir de qué hoja del Cuaderno nació. Esto respeta explícitamente la decisión ya ratificada en Sprint 3.5 ("Ideas y Operaciones nunca se mezclan ni se convierten una en la otra") — un puntero de procedencia no mezcla ni deriva campos, es trazabilidad pura, la misma función que `Idea.history` ya cumple una capa más abajo. El flujo de interacción que ese futuro sprint debería construir tampoco vive en el Cuaderno (el brief lo prohíbe explícitamente: "No buttons inside paragraphs. No badges. No labels. No module language.") sino en Threshold, extendiendo el mismo "Cambiar destino" que ya existe: al elegir Trading/Finanzas, pedir los pocos campos que `Idea` no tiene y `Operacion`/`FinanceAccount` sí (`instrumento`/`lado`, `tipo`/`saldo`), crear la entidad nueva con `origenIdeaId` apuntando a la idea de origen, y decidir recién ahí (no ahora) si la idea original se archiva o se conserva — pregunta deliberadamente abierta, no resuelta en este sprint.
**Se decidió no escribir ese campo todavía** porque hacerlo ahora — antes de que exista una sola línea de código que lo lea o lo escriba — sería exactamente el tipo de campo especulativo que las Reglas 12/13 de `AI_DEVELOPMENT_RULES.md` piden evitar ("nunca intentar completar una visión completa en un único sprint... implementar únicamente la primera etapa"): la primera etapa de mayor impacto y menor complejidad de "preparar una evolución que todavía no se construye" es el diseño documentado, no un campo muerto en cuatro tipos.
**Motivo:** instrucción explícita del dueño del proyecto (brief "EL ESTUDIO — SPRINT: THE SEED"): "Prepare ideas to evolve... Do not build AI. Do not build automation... Do not implement conversion yet. Only design the foundation... Design a future-proof data model and interaction flow only if small structural changes are needed. Avoid UI changes unless essential." Se investigó el modelo completo antes de decidir si hacía falta algún cambio, y se concluyó — con evidencia de código, no por default — que ninguno es necesario todavía: dos de los cuatro destinos ya están completos, y los otros dos necesitan un diseño, no una migración prematura.

---

**Fecha:** 2026-07-27
**Decisión:** (Pase visual — "EL ESTUDIO — VISUAL REFINEMENT", explícitamente no de arquitectura ni de workflows: "Bring El Estudio's UI as close as possible to the attached reference while preserving the current architecture. Do not redesign workflows. Refine the visual system."). Referencia (3 capturas + panel de análisis) tratada como dirección, no como pixeles ("Do not copy pixels. Capture its visual language."). **(1) Legibilidad (prioridad 1)**: se midió a mano el contraste WCAG de cada token dinámico contra `--canvas`/`--surface` en los DOS extremos del ciclo día/noche de `lightEngine.ts` (`computeLight()` interpola continuamente entre las tablas `NIGHT`/`DAY` — un chequeo de un solo extremo habría ocultado la mitad del problema). Resultado: `inkFaint` fallaba AA en ambos extremos (`NIGHT` ≈3.78:1, `DAY` ≈3.09:1 — este último peor de lo esperado, contra el canvas casi blanco del mediodía) y `accent` estaba marginal (`NIGHT` ≈7.40:1 bien, pero con margen ajustado). Se subió `NIGHT.inkFaint` de `#786D60` a `#8A7F70` (≈4.87:1) y se oscureció `DAY.inkFaint` (≈4.90:1) en la misma dirección de matiz — un mismo token no puede subir de luminosidad para la noche y bajar para el día con un solo valor estático, así que ambas tablas de `lightEngine.ts` se tocaron por separado, nunca un valor "de compromiso" a mitad de camino. `NIGHT.accent` adopta el hex exacto que propone la referencia (`#D8A24A`) tras confirmar por cómputo que mejora contraste (7.40:1→8.36:1) en vez de degradarlo — la regla del brief ("cuando referencia y UI actual difieran, preferir la referencia, salvo que dañe usabilidad") se satisface con un cambio verificado, no una copia ciega. `DAY.accent` se oscurece en la misma dirección (≈4.82:1) para no perder AA contra un canvas diurno casi blanco. Un segundo bug de legibilidad, más abajo del token: `IdeaCapture.tsx` apilaba un modificador `/80` de opacidad sobre `text-ink-faint` en el texto de confirmación ("Guardado."/vista previa de clasificación) — sobrevivía incluso después de arreglar el token porque el cálculo de contraste de un color con opacidad reducida mezclado contra el fondo es otro cómputo, no el mismo; se quitó el `/80` en los dos lugares. `src/index.css` (`:root`) y `estudio-design-system/tokens/colors.ts` (mirror documentado, cero consumidores hoy) se actualizaron con los mismos valores para no romper la convención de sincronía ya establecida entre los tres archivos. **(2) Cuaderno**: se mantiene el concepto minimalista — nada de textura de papel, cards, ni el acento de margen ya borrado en "Writing Comfort". Se agrega `.cuaderno-luz` (`position: fixed; inset: 0; z-index: -1; background: color-mix(in srgb, var(--canvas) 92%, white 8%)`) como primer hijo de `DiarioScreen`, siguiendo la misma convención de capa de viewport completo que ya usan `.room-layer-*` (nunca un "card" acotado) — a diferencia del scrim de oscurecimiento que "Writing Comfort" evaluó y descartó explícitamente por riesgo de leerse como página, este es un aclarado (no un oscurecimiento) y de viewport completo, no un rectángulo detrás del texto, así que no reintroduce ese riesgo. Sigue leyendo `--canvas` en vivo, así que acompaña automáticamente el ciclo de luz sin tocar `lightEngine.ts`. **(3) Home**: se confirmó por lectura de código (`HoyScreen.tsx`, `ContinueWorking.tsx`, `MisionPrincipal.tsx`, `Spaces.tsx`) que ninguna pantalla de Hoy ya muestra contenido de Cuaderno/`destino:'hoy'` — el brief pide "remove every automatic preview", pero no había ninguna que remover; se documenta como confirmación, no como cambio. **(4) Nav inferior**: `.nav-inferior` reemplaza el `bg-surface/95 border-t border-border/40 backdrop-blur` inline (la queja explícita del brief, "feels flat and transparent") por una placa tallada: `color-mix` con negro (no un tono nuevo) la separa levemente de `--surface`, un filo superior de luz (`inset 0 1px 0 rgba(255,255,255,.05)`) y una sombra que sube hacia el contenido (no hacia abajo, ya toca el borde del viewport) en vez de un simple hairline. El estado activo pasa de `text-ink` a `text-accent` para que "tocable/seleccionado" se lea por color, no solo por peso. **(5) Íconos**: sin librería nueva (restricción explícita del brief), 7 SVG a mano (`estudio-design-system/icons/ModuleIcons.tsx` — no `src/components/`, ver hallazgo de arquitectura abajo) con un solo lenguaje visual (24×24, trazo 1.5, sin relleno, puntas redondeadas), uno por destino real (`MODULE_ICONS` mapeado por `path`, la misma llave que ya usan `MODULES`/`SPACES`) y `currentColor` para heredar el mismo color que ya cambia el label de al lado (activo/inactivo), sin estado propio. Se usan en el nav de escritorio, el nav inferior y en `Spaces.tsx` (Espacios de Hoy). **(6) Tipografía**: la jerarquía se resuelve con los mismos cambios de token de (1) — `ink-faint` ya no compite en opacidad con `ink`/`ink-dim`, sin agregar ninguna escala de tamaño nueva. **(7) Atmósfera**: no se tocó ningún valor de `.room-layer-window`/`.room-layer-lamp`/opacidad global — la habitación sigue exactamente igual de cálida y oscura; solo los tokens de texto/acento cambiaron.
**Hallazgo de arquitectura**: el archivo de íconos se creó primero en `src/components/icons/` y se relocalizó a `src/packages/estudio-design-system/icons/` tras leer `.dependency-cruiser.cjs` — `module-no-app-tree`/`today-app-tree-boundaries` prohíben que un módulo de contenido (incluido `today`, de donde `Spaces.tsx` necesitaba importarlo) dependa de `src/components/*`; `estudio-design-system` es una hoja del árbol de dependencias que cualquier módulo puede importar sin violar esa regla. Verificado con `depcruise` limpio (115 módulos/193 dependencias).
**Hallazgo, no arreglado (fuera de alcance, Regla 3):** durante la verificación visual se descubrió que una navegación directa a `/diario` como PRIMERA carga de página (sin haber pasado antes por Hoy en la misma sesión de JS) deja la pantalla en blanco en Chrome headless: `useIdeas.ts` (Core V3) guarda `ready` como una variable de módulo simple, y `ideaRepository.list()` (IndexedDB) nunca resuelve en ese escenario específico de automatización, dejando `if (!ready) return null` bloqueado indefinidamente. Se confirmó por comparación directa (`git stash` + rebuild + captura) que el comportamiento es **idéntico en el baseline pre-sprint**, sin ningún cambio de este sprint aplicado — no es una regresión de "Visual Refinement". Se investigó también si era un artefacto del modo headless "legacy" (`--headless` vs `--headless=new`): el mismo resultado se reprodujo en ambos modos, así que no es eso. La ruta de uso real (abrir la PWA → Hoy monta `IdeaCapture`/`useIdeas()` → `load()` ya se disparó y resuelve antes de tocar Cuaderno en el nav) nunca pasa por el mismo camino que reproduce el bug en la prueba automatizada. Queda documentado para que un sprint futuro lo investigue (posible fix: derivar `ready` también vía `useSyncExternalStore` en vez de leerlo como variable de módulo suelta), no resuelto acá por ser preexistente y fuera del alcance de un sprint de refinamiento visual.
**Motivo:** instrucción explícita del dueño del proyecto (brief "EL ESTUDIO — VISUAL REFINEMENT"): "Writing must always be easier to read than the background... If the user needs to make an effort to read, the interface has already failed" (Prioridad 1) y "Whenever the reference image and the current UI disagree, prefer the reference, unless it hurts usability" — de ahí que el accent de la referencia se haya adoptado literalmente tras verificar que no dañaba (de hecho mejoraba) el contraste, y que el resto de la paleta propuesta (textos, fondo, superficie) se haya usado como guía de dirección para retocar tokens ya existentes, nunca para reemplazar el sistema de color completo (el brief pide "no copiar píxeles"). "No new libraries" y "Reuse Graphify. Do NOT regenerate Graphify unless structural changes are required" se cumplen: cero dependencias nuevas, y Graphify no se regeneró (mismos módulos, mismas relaciones — el único archivo nuevo, `ModuleIcons.tsx`, es un paquete hoja sin relaciones estructurales nuevas hacia el resto del árbol). El bug de `/diario` en pruebas automatizadas se documenta en vez de arreglarse en el mismo sprint porque no es un cambio de este brief (Regla 3, alcance mínimo) y porque arreglarlo a ciegas sin reproducirlo en un dispositivo real habría sido especulativo.
**Impacto:** 7 archivos modificados (`lightEngine.ts`, `index.css`, `estudio-design-system/tokens/colors.ts`, `IdeaCapture.tsx`, `DiarioScreen.tsx`, `AppShell.tsx`, `Spaces.tsx`) + 1 archivo nuevo (`estudio-design-system/icons/ModuleIcons.tsx`). Cero dependencias nuevas, cero cambios de esquema o de datos, cero regeneración de Graphify. `npm run verify` (build + boundaries + lint + test) pasa limpio: 115 módulos/193 dependencias cruzadas, sin violaciones de `dependency-cruiser`, 5/5 tests. Único hallazgo de `oxlint`: la misma advertencia pre-existente de Fast Refresh en `ContinueWorking.tsx` (ya documentada en sprints anteriores) más una equivalente nueva en `ModuleIcons.tsx` (exporta `MODULE_ICONS` junto a los componentes de ícono en el mismo archivo) — mismo patrón ya aceptado, no un error.
**Impacto:** cero archivos de `src/` modificados, cero dependencias nuevas, cero cambios de esquema, cero cambio de UI, cero regeneración de Graphify (no hubo ningún cambio estructural que la ameritara). Único archivo tocado: `docs/DECISIONS.md` (esta misma entrada). `npm run verify` no aplica cambios de código para re-verificar; se confirma igualmente que el build/lint/test siguen limpios desde el commit anterior. El despliegue de Vercel que dispara este commit es, a propósito, idéntico byte a byte al anterior — la prueba de que este sprint preparó el futuro sin aumentar la complejidad del presente.

---

**Fecha:** 2026-07-27
**Decisión:** (Pase visual — "EL ESTUDIO — PROJECT: VISUAL WEIGHT", explícitamente no de features ni de layout: "Do not add features. Do not redesign layouts. Only refine presence."). Referencia (1 imagen, sin panel de análisis esta vez) tratada como "confianza visual", no como píxeles a copiar. El brief pide juzgar con los ojos y con uso real de teléfono por encima del cálculo WCAG puro — se usó ambos en combinación: WCAG como piso ya garantizado por "Visual Refinement" (`inkFaint`/`accent` ya cumplían AA en los dos extremos del ciclo, verificado de nuevo en `lightEngine.ts` antes de tocar nada), y juicio visual directo (capturas de teléfono, 390×844) como árbitro de qué necesitaba más peso. **(1) Texto**: se encontraron y corrigieron dos defectos reales bajo el token, no en el token. Primero, `text-ink-faint/70` en `TradingScreen.tsx` (el botón "Editar plantilla") apilaba una opacidad extra sobre un token ya diseñado para ser el escalón más tenue — el mismo bug de opacidad-apilada que "Visual Refinement" ya había encontrado y corregido en otro lugar (`IdeaCapture.tsx`), pasado por alto acá; se quitó el `/70`. Segundo, el placeholder del campo de captura principal (`IdeaCapture.tsx`, "¿Qué tenés en mente?") y los 6 placeholders de edición en `FinanceScreen.tsx` usaban `--ink-faint` (el escalón terciario) para el primer texto que lee cualquiera al abrir una puerta vacía — el mismo razonamiento que "Writing Comfort" ya había aplicado al placeholder del Cuaderno (`--ink-dim`, ver comentario en `index.css` línea ~2773) nunca se extendió al resto de la app; ahora los 7 usan `--ink-dim`, igualando el criterio ya establecido en vez de inventar uno nuevo. Tres literales de color obsoletos (`rgba(206, 150, 92, ...)`, el accent previo a "Visual Refinement") sobrevivían pese a que el token `--accent`/`--accent-soft` ya se había actualizado: dos en `IdeaCapture.tsx` (sombra de foco/guardado) y uno en `index.css` (fallback de `.habito-dia-hoy`) — los tres pasan a `rgba(216, 162, 74, ...)`, el valor real del accent actual. **(2) Jerarquía visual**: se revisaron todos los divisores de opacidad baja (`border-border/40`, `/60` en `AppShell.tsx`, `RecentActivity.tsx`, `Spaces.tsx`, `FinanceScreen.tsx`) y se decidió NO tocarlos — son exactamente la aplicación correcta de "el fondo desaparece, nada compite con el contenido" (Prioridad 2 del propio brief); ganarles peso habría sido lo opuesto de lo pedido. Documentado como confirmación, no como omisión. **(3) Nav inferior**: `.nav-inferior` profundiza lo que "Visual Refinement" ya había tallado (más negro en el `color-mix` de fondo, de 94/6 a 90/10; sombra más oscura y más próxima; filo superior de luz reforzado) en vez de rediseñarlo, y se agrega `.nav-inferior-item-activo` — un cuerpo propio (`--accent-soft`, la misma pareja de color que ya usa `.habito-dia-hoy`) detrás del ítem activo en `AppShell.tsx`, para que "estoy acá" se lea como una pieza tallada en el panel, no solo un cambio de tinta sobre la misma superficie plana. **(4) Botones**: se encontró el mismo defecto repetido cinco veces — el botón "Guardar" de cada formulario primario (`IdeaCapture.tsx`, 3× en `FinanceScreen.tsx`, 1× en `TradingScreen.tsx`) era texto de color puro, sin fondo, sin borde, sin forma: la definición exacta de "flat UI" que el brief pide reducir (Prioridad 4). Se creó una única clase nueva, `.accion-primaria` (fondo `--accent-soft` + borde `color-mix` con `--accent` + texto `--accent` + sombra + estado `:active` con escala), reusada en los cinco lugares en vez de repetir la regla cinco veces (Regla 4). Se eligió `--accent-soft` como fondo (en vez de un `--accent` sólido) deliberadamente: como `--accent` se interpola día/noche y en pleno día es un marrón oscuro mientras que de noche es un dorado claro (`lightEngine.ts`, `DAY.accent` vs `NIGHT.accent`), un botón de fondo sólido habría necesitado recalcular el color del texto en cada extremo; `--accent-soft` (ya emparejado con texto `--accent` en `.habito-dia-hoy`, patrón existente) mantiene el fondo cerca de `--surface` en los dos extremos, así que el contraste texto/fondo se mantiene sin matemática nueva. Los secundarios (`Cancelar`, `.idea-destino`, `.idea-proposal-target`) se revisaron y se dejaron exactamente como estaban — llevan el comentario explícito "nunca un botón con borde" (`index.css` línea ~1118) documentando que esa falta de peso ya era una decisión deliberada de jerarquía (Prioridad 2: "secondary content supports"), no un defecto a corregir. El punto de hábito (`.habito-punto`, en `HabitsGlance.tsx`) tenía el mismo problema en miniatura: un glyph (●/○) flotando solo dentro de un área táctil invisible de 44px, sin cuerpo propio en ningún estado; gana un círculo de fondo (tenue sin marcar, `--accent-soft` marcado) sin tocar el glyph, el tamaño del área táctil ni la lógica de `toggle`. **(5) Íconos**: se revisó el set de 7 íconos a mano (`ModuleIcons.tsx`) contra los criterios del brief (alineación, balance óptico, consistencia de trazo) — ya comparten un solo trazo (1.5) y viewBox (24×24) desde "Visual Refinement" y se juzgó, mirándolos, que ya están en equilibrio; no se encontró una razón concreta para tocarlos, así que se documenta como revisión confirmada, no como cambio pendiente. **(6) Cuaderno**: ya había recibido en "Visual Refinement" tanto el placeholder de mayor contraste (`--ink-dim`) como la capa `.cuaderno-luz` (fondo aclarado un 8% de viewport completo, sin textura de papel); este sprint no encontró nada adicional que cambiar sin violar la restricción explícita del propio brief ("Do not add paper textures. Do not add notebook effects. The room is already the environment."). Documentado como confirmación. **(7) Home**: se confirmó de nuevo por lectura de código que ninguna pantalla de Hoy muestra contenido de Cuaderno — sigue siendo cierto desde "Visual Refinement", sin cambios necesarios.
**Hallazgo, no arreglado (fuera de alcance, Regla 3, continuación):** se retomó la investigación del bug ya documentado de `/diario` en blanco bajo Chrome headless (ver entrada anterior) para confirmar si era una carrera lenta en vez de un cuelgue permanente — se probó con `--virtual-time-budget=20000` (20 segundos de tiempo virtual, contra los 4-6 segundos de la vez anterior) y el resultado fue idéntico: pantalla en blanco, cero contenido de Cuaderno. Esto descarta "solo necesita más tiempo" como explicación y refuerza que la promesa de IndexedDB de `ideaRepository.list()` genuinemente nunca resuelve en ese escenario específico de navegación directa. Durante las capturas de este sprint se observó además que el mismo patrón de pantalla en blanco aparece de forma intermitente en `/habitos`, `/trading` y `/finanzas` bajo navegación directa (pero no en `/misiones`, que sí renderizó su estado vacío correctamente) — consistente con la causa ya identificada (`useIdeas.ts`: `ready` es una variable de módulo simple, no pasa por `useSyncExternalStore`), no un hallazgo nuevo ni una regresión de este sprint. Sigue sin arreglarse por ser preexistente y fuera del alcance de un sprint de peso visual — se documenta la extensión de la investigación para que quede registrada la evidencia adicional (20s no alcanza; el patrón no es exclusivo de Cuaderno) de cara a un futuro sprint que sí lo aborde.
**Motivo:** instrucción explícita del dueño del proyecto (brief "EL ESTUDIO — PROJECT: VISUAL WEIGHT"): "If reading requires effort, the interface has failed... If an element feels transparent, increase its visual weight... Real usage has priority over WCAG calculations. Design with your eyes." De ahí que los cambios de este sprint prioricen defectos encontrados por inspección visual y de código (opacidad apilada, literales de color obsoletos, botones sin forma) por sobre un nuevo pase de cálculo de contraste — el piso WCAG ya estaba resuelto por el sprint anterior y se confirmó, no se repitió. "Reuse Graphify. Do NOT regenerate Graphify unless structural changes are required" y "No new libraries" se cumplen: cero dependencias nuevas, cero archivos nuevos, Graphify no se regeneró (ninguna relación estructural nueva — un solo selector CSS compartido y clases aplicadas a componentes ya existentes). "Do not add features. Do not redesign layouts" se cumple: cada cambio es una clase CSS nueva o un ajuste de token sobre elementos que ya existían en el mismo lugar, mismo tamaño, misma lógica.
**Impacto:** 5 archivos modificados (`index.css`, `IdeaCapture.tsx`, `FinanceScreen.tsx`, `TradingScreen.tsx`, `AppShell.tsx`). Cero archivos nuevos, cero dependencias nuevas, cero cambios de esquema o de datos, cero regeneración de Graphify. `npm run verify` (build + boundaries + lint + test) pasa limpio: 115 módulos/193 dependencias cruzadas, sin violaciones de `dependency-cruiser`, 5/5 tests. Único hallazgo de `oxlint`: las mismas dos advertencias pre-existentes de Fast Refresh (`ContinueWorking.tsx`, `ModuleIcons.tsx`), sin advertencias nuevas.
